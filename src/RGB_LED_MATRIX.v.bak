/*
 *
 */
 
`define number_of_bits 16
`define n `number_of_bits-1
`define matrix 25
`define m `matrix-1

module rgb_led_matrix(
	input 				clk,
	input					reset,
	output[matrix:0] 	rgb,
	output[2:0]			leds
);

	reg[`n:0]

endmodule